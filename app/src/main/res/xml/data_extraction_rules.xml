<?xml version="1.0" encoding="utf-8"?>
<data-extraction-rules>
    <!--
        Exclude the DataStore preferences file from cloud backup and device transfer.
        It contains server configuration, download directory URIs (not valid across
        installs), and file history (references files that expire after one hour).
    -->
    <cloud-backup>
        <exclude domain="file" path="datastore/"/>
    </cloud-backup>
    <device-transfer>
        <exclude domain="file" path="datastore/"/>
    </device-transfer>
</data-extraction-rules>
