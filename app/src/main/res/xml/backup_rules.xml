<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!--
        Exclude the DataStore preferences file. It contains server configuration,
        download directory URIs (which are not valid across installs), and file
        history (which references files that expire after one hour).
    -->
    <exclude domain="file" path="datastore/"/>
</full-backup-content>
